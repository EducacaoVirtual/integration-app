server:
  port: 8090

spring:
  application:
    name: shippimentapp

  datasource:
    url: jdbc:mysql://localhost:3306/shippimentapp?createDatabaseIfNotExist=true
    username: root
    password: educacaovirtual2025
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
